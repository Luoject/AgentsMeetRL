# 准备并提交 Pull Request

把当前工作整理成清晰的 PR。

## 要求

1. 确认分支、变更范围与 base（默认 `main`）。
2. 如有未提交改动：整理后 commit（信息简洁、说明 why）。
3. push 到远程，创建或更新 PR。
4. PR 标题与正文用简体中文，包含：
   - 做了什么
   - 为什么做
   - 如何验证
5. 不要把密钥、SMTP 密码、个人令牌写进正文或提交。
6. 完成后给出 PR 链接与简短摘要。
